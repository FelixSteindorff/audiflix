# Pinned dependency set for reproducible Windows release builds of v0.1.0.
# Update deliberately, then rebuild and record the new SHA-256 in CHANGELOG.md.
wxPython==4.3.1
python-vlc==3.0.21203
requests==2.34.2
keyring==25.7.0
accessible_output2==0.17
pyinstaller==6.22.2
